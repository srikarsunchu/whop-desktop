{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "The local React frontend (main window) gets core defaults, window dragging, and the app's own commands, which shell out to the Whop CLI. There is NO `remote` allowlist: the optional `web` window that loads whop.com never gets Tauri IPC.",
  "platforms": ["macOS"],
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:window:allow-internal-toggle-maximize",
    "core:window:allow-set-title"
  ]
}
